<script>
    // This is a way to watch Batman Beyond, and Mr. Bean (for now) in artistic ways
    // This is geared for music samplers.
    // I will have .mp4s for both shows
    // This player must quickly efficiently load the shows
    // It features reverse playback, fast forward,
    // creating your own named checkpoints (cached in local storage)
    // , must be able to easily clip out a portion of the episode
    // clip out to a sub mp4 or mp3
    // Skip intro feature (the shows should have the same skip point for every file)
    // 2x, 3x, 4x speed playback
    // Visual filters on the playback (greyscale, monochrome, high contrast)
    // Audio filters on the playback ('funny mic', vocal enhanced, reverb)
    // The shows are also, in my opinion, perfect. Stats on each episode
    // actors, filming locations, budget, fun facts, trivia,
    // release date, rating, viewer count (in millions), total run time
    // ratings, links to rotten tomatoes/imdb/wikipedia,
    // All this must be presented with a quick, polished, snappy, awesome UI

    // Videos and episode data should be cached locally, and this must be displayed in the UI
    // The UI should be super modern, smooth, and fast

    // Take extra care to ensure the layout works effieciently on mobile, pc, and also television internet browsers

    // Keyboard controls would also be very nice

    // Integrate the clipping feature with the other Soundboard / sampler app?
    // We will be expanding that Soundboard app to be more like a full fledged Teenage Engineering OP1 / Novation Launchpad

    const vids = ["Batman Beyond", "Mr. Bean", "The Walking Dead S1"];

    let currentVideoCategory = vids[0];
</script>
