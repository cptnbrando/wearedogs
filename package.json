{
  "name": "wearedogs",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "npm run changelog && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "changelog": "node scripts/compile-changelog.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tailwindcss/vite": "^4.3.0",
    "gh-pages": "^6.3.0",
    "sass-embedded": "^1.100.0",
    "svelte": "^5.55.5",
    "tailwindcss": "^4.3.0",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@breezystack/lamejs": "^1.2.3",
    "@lottiefiles/dotlottie-wc": "^0.9.19",
    "@threlte/core": "^8.0.1",
    "@threlte/extras": "^9.0.0",
    "@xenova/transformers": "^2.17.2",
    "fflate": "^0.8.3",
    "jsqr": "^1.4.0",
    "lucide-svelte": "^1.0.1",
    "marked": "^18.0.7",
    "qrcode": "^1.5.4",
    "svelte-i18n": "^4.0.1",
    "tesseract.js": "^7.0.0",
    "three": "^0.184.0"
  },
  "blahblahblah": {
    "if you see this shit": "npm warn allow-scripts 4 packages have install scripts not yet covered by allowScripts:",
    "npm warn allow-scripts Run npm approve-scripts --allow-scripts-pending to review, or npm approve-scripts <pkg> to allow": "npm warn allow-scripts   @parcel/watcher@2.5.6 (install: node-gyp rebuild) npm warn allow-scripts   core-js@3.49.0 (install: (install scripts present)) npm warn allow-scripts   es5-ext@0.10.64 (install: (install scripts present)) npm warn allow-scripts   esbuild@0.19.12 (install: (install scripts present))",
    "i dont fuck with that shit at all": "that shit annoy me",
    "needing a script to install shit before using the shit": "what the fuck",
    "and i dont know what the scripts do": "they could do literally anything. i have not installed the scripts personally.",
    "i just watch the warning come and go": "all fucking day",
    "...": "...",
    "anyway": "",
    "if you need batman you can run sh ./public/batman/download.sh": "toodle loo",
    "@breezystack/lamejs": "Used for catalytic converter to go to LAME mp3",
    "fflate": "Needed for catalytic converter to open zip files",
    "@lottiefiles/dotlottie-wc": "Used for dog logo animation",
    "tesseract.js": "Used for OCR image to text",
    "@xenova/transformers": "Used for Wiretap to transcribe MP3 and audio files 100% offline in browser using Whisper",
    "marked": "Markdown parser for the fundraiser bios in src/data/fundraiser/bios/. Reason: StorePanel had three hand-rolled regex passes to turn <a href> markup, <url> brackets and bare pasted URLs into links, and a bare URL silently rendered as dead text. One parser replaces all three, so bios are now plain markdown and inline formatting works for free. No install scripts."
  }
}
